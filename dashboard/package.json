{
  "name": "buy-low-sell-high-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && rm -rf dist/public && mkdir -p dist && cp -R public dist/public",
    "dev": "npm run build && node dist/server.js",
    "start": "node dist/server.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "sql.js": "^1.14.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
