name: buylowsellhigh

services:
  postgres:
    container_name: buylowsellhigh-postgres
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-buy_low_sell_high}
      POSTGRES_USER: ${POSTGRES_USER:-buy_low_sell_high}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  engine-worker:
    container_name: buylowsellhigh-engine-worker
    build:
      context: .
      dockerfile: engine/Dockerfile
    working_dir: /app
    command: ["python", "-m", "buy_low_sell_high.cli", "worker", "smoke"]
    environment:
      PYTHONPATH: /app/engine/src
    volumes:
      - ./:/app
    depends_on:
      - postgres

  engine-cli:
    container_name: buylowsellhigh-engine-cli
    build:
      context: .
      dockerfile: engine/Dockerfile
    working_dir: /app
    command: ["python", "-m", "buy_low_sell_high.cli", "bootstrap-check"]
    environment:
      PYTHONPATH: /app/engine/src
    volumes:
      - ./:/app
    depends_on:
      - postgres

  dashboard:
    container_name: buylowsellhigh-dashboard
    build:
      context: .
      dockerfile: dashboard/Dockerfile
    working_dir: /app/dashboard
    command: ["node", "--experimental-strip-types", "src/server.ts"]
    environment:
      PORT: ${DASHBOARD_PORT:-3232}
      DATABASE_URL: postgresql://${POSTGRES_USER:-buy_low_sell_high}:${POSTGRES_PASSWORD:-change-me}@postgres:5432/${POSTGRES_DB:-buy_low_sell_high}
    ports:
      - "${DASHBOARD_PORT:-3232}:3232"
    volumes:
      - ./:/app
    depends_on:
      - postgres

volumes:
  postgres-data:
    name: buylowsellhigh-postgres-data
