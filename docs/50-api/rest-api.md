# REST API

The Node dashboard is still a skeleton in this environment, but the intended API groups are wired as route stubs:

- `/api/data`
- `/api/profiles`
- `/api/backtests`
- `/api/manual`

All future API responses must carry reproducibility metadata.
