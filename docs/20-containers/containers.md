# Containers

- `postgres`: runtime store for market data, backtests, jobs, and manual ledger
- `engine-worker`: Python worker and CLI entrypoint
- `dashboard`: TypeScript Express API and static UI host

Redis is intentionally excluded.

